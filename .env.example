# docker compose が読み込む環境変数。
# このファイルを .env にコピーして使う（.env は .gitignore 済み）。
#
#   cp .env.example .env

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=invoice

# ホスト側の公開ポート。既に使っている場合は変更する
DB_PORT=5432
API_PORT=8000
WEB_PORT=3000
